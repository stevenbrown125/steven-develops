/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/steven-develops/",
  output: "export",
  images: { unoptimized: true },
  distDir: "dist",
};

module.exports = nextConfig;
